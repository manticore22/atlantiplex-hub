Build Tokens CSS
- This script reads design-tokens.json and emits tokens.css with CSS variables.
- Run: node build-tokens-css.js
- Output: design-system/token.css (overwrites existing tokens.css)
- Ensure you commit generated tokens.css as part of the patch or CI runs this on build.
